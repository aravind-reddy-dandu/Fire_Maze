maze = generateGrid(dim, fillProb) // generate grid with dimensions
solution = initialsolution for grid
firecell = generate a new random point
while not in fire
   maze = spreadFire(normal) // Spread fire normally
   solution = getsolution from current point
   if no solution:
		return dead
   point = solution[1]
   if point is destination:
	    return alive